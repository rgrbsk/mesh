<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <BlazorDisableThrowNavigationException>true</BlazorDisableThrowNavigationException>
    <!-- NU1902: vuln. moderada do AngleSharp, transitivo do BlazorBlueprint.Components
         (fora do nosso controle). Suprimido pra não abortar o Package Manager Console,
         que roda com ErrorActionPreference=Stop e trata warnings como fatais. -->
    <NoWarn>$(NoWarn);NU1902</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BlazorBlueprint.Components" Version="3.15.0" />
    <PackageReference Include="BlazorBlueprint.Icons.Lucide" Version="2.0.2" />
    <PackageReference Include="BlazorBlueprint.Primitives" Version="3.15.0" />
    <PackageReference Include="Microsoft.AspNetCore.Identity.EntityFrameworkCore" Version="10.0.0" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.0">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.EntityFrameworkCore.Tools" Version="10.0.0">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.0" />
    <PackageReference Include="Scrutor" Version="7.0.0" />
  </ItemGroup>

  <ItemGroup>
    <Folder Include="Localization\UserNetworkInfo\" />
    <Folder Include="Migrations\" />
  </ItemGroup>

  <!-- Lista de municípios do IBGE, lida uma vez pelo seed. Vai embutida no
       assembly em vez de ficar em wwwroot: é dado de referência do servidor, não
       arquivo para o navegador baixar. -->
  <ItemGroup>
    <EmbeddedResource Include="Data\cidades-ibge.csv" />
  </ItemGroup>

  <!-- Regenerate the Tailwind CSS on build (skipped if npm deps aren't installed). -->
  <Target Name="TailwindBuild" BeforeTargets="Build" Condition="Exists('node_modules')">
    <Exec Command="npm run css" WorkingDirectory="$(MSBuildProjectDirectory)" ContinueOnError="true" />
  </Target>

</Project>
